# Production build config for Milk Garage.
# These are the PUBLIC Supabase URL + publishable (anon) key — safe to ship in
# the browser bundle; access is enforced by Row-Level Security. The service_role
# key must NEVER appear here or anywhere in the repo.
# Overridden by Vercel project env vars of the same name if those are set.
VITE_SUPABASE_URL=https://pcwelnsubcbmftksnsjf.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_DiQVhAILpL_gmIhrABYBHA_Ffi2Tk7s
